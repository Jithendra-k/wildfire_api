options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/code-for-planet/wildfire-repo/${_SERVICE_NAME}:$COMMIT_SHA', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/code-for-planet/wildfire-repo/${_SERVICE_NAME}:$COMMIT_SHA']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run', 'deploy', '${_SERVICE_NAME}',
        '--image', 'us-central1-docker.pkg.dev/code-for-planet/wildfire-repo/${_SERVICE_NAME}:$COMMIT_SHA',
        '--region', 'us-central1',
        '--platform', 'managed',
        '--allow-unauthenticated',
        '--memory', '16Gi',
        '--cpu', '4'
      ]

images:
  - 'us-central1-docker.pkg.dev/code-for-planet/wildfire-repo/${_SERVICE_NAME}:$COMMIT_SHA'
